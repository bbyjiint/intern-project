services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: intern-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-intern_website}
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    volumes:
      # Persist database data
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - intern-network

  # Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: intern-backend
    env_file:
      - ./backend/.env
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      PORT: ${PORT:-5001}
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/${POSTGRES_DB:-intern_website}?schema=public
      FILE_STORAGE_PROVIDER: ${FILE_STORAGE_PROVIDER:-local}
      LOCAL_UPLOAD_DIR: /app/uploads
    ports:
      - "${BACKEND_PORT:-5001}:5001"
    volumes:
      # Mount uploads directory to persist files
      - ./backend/uploads:/app/uploads
      # Note: node_modules is NOT mounted to avoid platform conflicts (Windows vs Linux)
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - intern-network
    # Generate Prisma Client and start dev server
    command: sh -c "npm run prisma:generate && npm run dev"

  # Frontend (Next.js)
  frontend:
    build:
      context: ./frontend-next
      dockerfile: Dockerfile
    container_name: intern-frontend
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://localhost:5001}
      # For development, Next.js needs to watch files
      WATCHPACK_POLLING: ${WATCHPACK_POLLING:-false}
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    volumes:
      # Mount source code for hot reload (exclude node_modules for performance)
      - ./frontend-next:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      - backend
    networks:
      - intern-network
    command: npm run dev

volumes:
  postgres_data:
    driver: local

networks:
  intern-network:
    driver: bridge
